# Runtime dependencies for the FastAPI app and the seed scripts.
# (In the Docker image, CPU-only torch is installed first; see Dockerfile.)
fastapi==0.141.1
uvicorn[standard]>=0.30
python-dotenv>=1.0
pydantic>=2.11.7
portkey-ai==2.3.4
neo4j==6.3.1
lancedb==0.39.0
pyarrow>=16
sentence-transformers==6.1.0
